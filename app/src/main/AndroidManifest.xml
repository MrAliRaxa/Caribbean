<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codecoy.caribbean">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Caribbean">
        <activity android:name=".Activities.CountryIntro">



        </activity>
        <activity android:name=".Activities.LocalTourist" />
        <activity android:name=".Activities.TourismTips" />
        <activity android:name=".Activities.TouristTypeSelection" />
        <activity android:name=".Activities.CountriesSelection" />
        <activity android:name=".Activities.IntroScreen" />
        <activity android:name=".Activities.EmailVerification" />
        <activity android:name=".Activities.SignUp" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity" />
    </application>

</manifest>